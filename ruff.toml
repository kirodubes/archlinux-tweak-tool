line-length = 120

[lint]
# Pinned to the classic defaults: ruff 0.16 widened the implicit rule set and the pre-commit gate started failing on untouched code
select = ["E4", "E7", "E9", "F"]
ignore = [
    "E402",  # gi.require_version() must precede gi.repository imports
]
